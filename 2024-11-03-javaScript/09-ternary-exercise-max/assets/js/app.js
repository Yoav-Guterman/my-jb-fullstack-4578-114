const num1 = +prompt("enter a num1")
const num2 = +prompt("enter a num2")

const max = num1 > num2 ? `first number is bigger: the number is ${num1}` : `second number is bigger, the number is: ${num2}`;
document.write(max)