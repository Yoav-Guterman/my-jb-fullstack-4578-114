const numberOfDigits = +prompt('enter number of digits')

console.log('hello1')

const pi = Math.PI.toFixed(numberOfDigits)
// if insert more than 100 digits or less than 0 the function will not run properly and send run error

console.log('hello2')

console.log(pi)

