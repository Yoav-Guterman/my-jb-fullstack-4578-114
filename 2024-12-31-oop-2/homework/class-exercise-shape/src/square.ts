import Shape from "./shape.js";

export default class Square extends Shape {
    constructor(
        private height: number
    ) {
        super()
    }
    getArea(): number {
        return
    }
}